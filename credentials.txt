# API keys and model configuration
# paste API keys with no quotes

# OpenAI settings
openai_api_key=your_api_key
#can replace with any openai model, but this, or newer seems to perform the best
openai_model=gpt-4o 

# Anthropic settings - For me, this has yielded best performance
anthropic_api_key=your_api_key
#outstanding performance with 3_7
anthropic_model=claude-3-7-sonnet-latest

# Ollama settings - I've had mixed results with local smaller models, but has still worked.
ollama_model=model_from_ollama_list
